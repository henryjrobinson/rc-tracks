# Ralph Progress Log — rc-tracks Trip Planner

## Codebase Patterns
- All filter state lives in `app/track-browser.tsx`; child components are presentational.
- The view-toggle pattern is in track-browser.tsx — extend it for new views.
- `app/map-view.tsx` is dynamically imported with `ssr: false` to avoid Leaflet/window crashes.
- Geo helpers (geocoding, haversine, geolocation, network calls) belong in `lib/geo.ts`.
- localStorage caches use a versioned key (e.g. `rc-tracks:geocode-cache:v1`) and JSON.parse with try/catch.
- The Track type is in `site/lib/tracks.ts`. The data file is `site/data/tracks.geocoded.json`.
- All styles in `site/app/globals.css`. Mobile-first; `@media (min-width: 720px)` for desktop.
- Touch targets ≥44px (use `min-height: 44px` for inputs/buttons).
- Map markers use `L.icon({ iconUrl: 'https://unpkg.com/leaflet@1.9.4/dist/images/marker-icon.png', ... })` to avoid bundler issues.
- Build verifies with `cd site && npx tsc --noEmit && npm run build`.

Started: ralph initialization

---

## 2026-05-09T01:15:49Z - TRIP-1
- Added a third "Trip" tab to the view toggle in track-browser.tsx and rendered a new <TripPlanner /> stub when active.
- TripPlanner has start/destination text inputs, corridor selector (5/10/25/50/100 mi, default 25), and a Plan-trip button that currently only logs.
- Added .trip-panel / .trip-form / .trip-field / .trip-submit / .trip-empty CSS in globals.css; mobile-first single column, 2-col + selector + button at ≥720px.
- Files modified: site/app/track-browser.tsx, site/app/trip-planner.tsx (new), site/app/globals.css.
- **Learnings:**
  - The view-toggle uses a ternary for list vs map; switching to per-view `&&` blocks scales cleanly to a third (and future fourth) view without nested ternaries.
  - `View` union type is the single source of truth — extend it before adding tab buttons or render branches.
  - When TripPlanner needs to read filtered tracks or call back into TrackBrowser (TRIP-4+), pass them as props from track-browser.tsx; don't duplicate filter state inside TripPlanner.
  - Keep TripPlanner in its own file (already imported) so future stories can extend its state without bloating track-browser.tsx.
---

## 2026-05-08T00:00:00Z - TRIP-2
- Added `Route` type and async `getRoute(start, end, signal?)` to lib/geo.ts. Calls OSRM public demo (`router.project-osrm.org`) with `overview=full&geometries=geojson&steps=false`. Maps GeoJSON `[lon, lat]` pairs to `{ lat, lon }`, converts meters→miles and seconds→minutes. Returns null when no route is found; throws on HTTP errors so callers can show a friendly message.
- Files modified: site/lib/geo.ts.
- **Learnings:**
  - GeoJSON coordinates are `[lon, lat]` — remember to swap when constructing `LatLon`. The existing codebase consistently uses `{ lat, lon }`, so any new geo helper should normalize at the boundary.
  - OSRM's public demo has no auth and rate limits per IP. It's fine for a hobby site but TRIP-4 should AbortController-cancel in-flight requests on resubmit (already in story acceptance criteria).
  - Don't trust `routes[0].geometry?.coordinates` to be defined — typed as optional in the response type. Guard before mapping.
  - Conversion constant: 1 mile = 1609.344 m exactly; using a literal avoids importing/exporting another constant.
---
## 2026-05-08T00:00:00Z - TRIP-3
- Added `pointToPolylineMiles(p, polyline)` plus a private `closestPointOnSegment` helper to lib/geo.ts.
- Empty polyline → Infinity; single-point polyline → haversine to that point; else iterate consecutive segments.
- Per-segment projection uses an equirectangular approximation: scale longitudes by cos(midLat) so the segment becomes a flat 2D line, project p onto it, clamp t∈[0,1], then haversine the closest lat/lon back.
- Files modified: site/lib/geo.ts.
- **Learnings:**
  - For the corridor test (TRIP-4+), each candidate track only needs one representative LatLon, so callers can pass `[track.location]`-style polylines OR the full OSRM route — the helper is symmetric in roles. The PRD wording suggests we'll pass `pointToPolylineMiles(trackLatLon, route.polyline)`.
  - Equirectangular is plenty accurate for trip-corridor distances (<100 mi). No need for spherical law of cosines per segment — the haversine on the closest projected point is the great-circle measurement that matters.
  - We scale by `cos(midLat)` not `cos(meanLat)` — works because OSRM polylines are dense enough that each segment spans tiny degrees. Avoid converting to meters explicitly; the unit cancels in the t-formula.
  - Keep the helper pure (no fetch, no globals) so TRIP-4 can call it inside a `useMemo`.
---

## 2026-05-08T00:00:00Z - TRIP-4
- Added `nearestOnPolylineMiles(p, polyline)` to lib/geo.ts: returns both the perpendicular detour and the cumulative along-route distance to the closest point. Reuses the private `closestPointOnSegment` from TRIP-3.
- Passed `tracks` from TrackBrowser to TripPlanner (`<TripPlanner tracks={tracks} />`) so the planner can filter without re-loading data.
- Replaced the stub click handler in trip-planner.tsx with a real pipeline: validate inputs → AbortController.abort() previous → `Promise.all([geocode(start), geocode(dest)])` → `getRoute()` → filter tracks (lat/lon present, detour ≤ corridor) → sort by `alongMi`. Stored a `TripResult` in component state.
- Added loading state (`Planning…` button label, disabled inputs), friendly error copy for each failure mode (geocode miss, no route, zero matches, network error), and `setError(null)` on each retry.
- Added `.trip-error`, `.trip-summary`, and disabled-state styles to globals.css.
- Files modified: site/lib/geo.ts, site/app/track-browser.tsx, site/app/trip-planner.tsx, site/app/globals.css.
- **Learnings:**
  - The pattern for AbortController + finally: only clear `setLoading(false)` when `abortRef.current === ctrl` so a superseded older promise doesn't flip the spinner off mid-flight of a newer request. Same pattern will apply to TRIP-7+ if it triggers from elsewhere.
  - `nearestOnPolylineMiles` accumulates segment lengths via `haversineMiles(a, b)` — fine for OSRM polylines (a few hundred points). If TRIP-6 needs both detour and along, prefer this single helper over calling `pointToPolylineMiles` then re-walking the polyline.
  - I returned `setResult(null)` *and* an error string for the zero-match case so TRIP-6 can decide whether to render the empty-results card or the error banner. Currently the error banner shows; TRIP-6 may want to flip this so a result with `matches:[]` renders the empty state instead.
  - Geocode/route errors propagate as `Error.message`. Nominatim and OSRM both have rate limits; the existing localStorage geocode cache helps the start/dest pair but route requests are not cached. If we hit limits in TRIP-7+, consider caching `getRoute` by `${start.lat},${start.lon}-${end.lat},${end.lon}` for the session.
  - `'use client'` directive must stay first; importing types-only (`type Route`) avoids breaking the SSR boundary.
---

## 2026-05-09T01:24:18Z - TRIP-5
- Extended MapView with optional `route?: LatLon[] | null` and `highlightedSlugs?: Set<string> | null` props. Route renders as a Leaflet `Polyline` (accent color, weight 4) and a new `<FitToRoute>` child fits the polyline bounds with 40px padding. Highlighted markers use a new `HIGHLIGHT_ICON` (L.divIcon → `.trip-marker` / `.trip-marker-pin` CSS) and get `zIndexOffset: 1000` so they sit above clusters and the standard markers.
- Made the focus behavior mutually exclusive: when a route is provided FitToRoute wins; otherwise FocusOnOrigin still flies to the origin pill. The radius circle still draws regardless, in case the user has both an origin filter and a planned trip.
- Lifted the trip-result state up to TrackBrowser via an `onPlanned` callback exported from TripPlanner. TripPlanner publishes its current `result` (or `null` on retries / failures) through a `useEffect`, and TrackBrowser memoizes a `tripHighlightedSlugs: Set<string>` plus the polyline reference, then passes both to MapView. Switching to the Map tab after planning now shows the route + orange dots automatically.
- Added a "N on trip route" suffix to the existing `.map-summary` text via `.map-summary-highlight` so users can tell the highlight is intentional.
- Files modified: site/app/map-view.tsx, site/app/track-browser.tsx, site/app/trip-planner.tsx, site/app/globals.css.
- **Learnings:**
  - `L.divIcon({ className: 'trip-marker' })` REPLACES the default `leaflet-div-icon` class entirely (no `leaflet-div-icon` background-white style to fight). If a future story needs to keep the default styling, use `className: 'leaflet-div-icon trip-marker'` instead.
  - `L.markerClusterGroup` needs to be torn down + rebuilt on `highlightedSlugs` change because we set the icon at marker-creation time. Adding `highlightedSlugs` to the MarkerCluster effect deps does this — cheap because tracks lists are <2k entries.
  - For the parent→child→parent state lift, `useEffect(() => onPlanned?.(result), [result, onPlanned])` works because React guarantees `setState` callbacks are stable. If a non-setter is ever passed as `onPlanned`, wrap in `useCallback` at the call site or this effect will refire endlessly.
  - The `setTripResult(null)` reset is implicit: TripPlanner now also passes `null` whenever it calls `setResult(null)` on retry, which keeps the map in sync. TRIP-7 ("Show on map" button + clear) should consider exposing a "clear trip" action somewhere visible in the map view too.
  - `<Polyline positions>` from react-leaflet wants `[lat, lon][]`, not `LatLon` objects — we convert once in MapView so callers can keep passing the polyline straight from `Route`.
---

## 2026-05-09T01:27:11Z - TRIP-6
- Replaced the single-line `.trip-summary` with a full results section. Header shows `{distanceMi} mi · {formatDuration(durationMin)}` with the matching count + corridor on the right; below that, an `.list` of `.card` entries reusing the existing track-card markup. Each entry shows track name (link to track.url), city/state/countryCode, surface tag (when known), an `indoor` tag, and a `card-meta` row with Website + Google-Maps Map link. Detour mileage is rendered as `{m.detourMi.toFixed(1)} mi off route` next to the location, in the accent color via `.trip-detour`.
- Removed the `setError(...)` for the zero-match case — the empty state ("No tracks within X mi… try a wider corridor.") now renders inside the results section so users still see the route stats and matched count instead of an error banner.
- Added a small private `formatDuration(minutes)` helper: `< 60 → "X min"`, else `"H hr"` or `"H hr M min"`.
- Added CSS: `.trip-results`, `.trip-results-header`, `.trip-results-stats`, `.trip-results-divider`, `.trip-results-count`, `.trip-results-list`, `.trip-results-empty`, `.trip-detour`. Used existing `.list` + `.card` for the result entries so the visual matches the regular browse view.
- Files modified: site/app/trip-planner.tsx, site/app/globals.css.
- **Learnings:**
  - The TRIP-4 author left a deliberate fork-in-the-road on zero-match handling (banner vs. empty state inside results). TRIP-6 picks the empty-state path; if TRIP-7+ ever needs the banner back for a different reason, the existing `.trip-error` class is still there.
  - Re-implementing the card markup inside trip-planner.tsx (rather than exporting `TrackCard` from track-browser.tsx) keeps each view's card free to diverge — TripResultCard hides phone/email and swaps `.distance` for `.trip-detour` without touching the regular browse card.
  - `match.detourMi.toFixed(1)` returns a trailing-zero string (e.g. `0.0 mi off route`). Acceptable for the corridor scale; if TRIP-8 later wants `<0.05 → "on route"` copy, it'll need a small ternary.
  - Counts use the same singular/plural ternary pattern (`tracks within ...`) as the older summary so screen readers (and a future aria-live in TRIP-8) get a consistent string shape.
  - `formatDuration` is intentionally local (not exported from lib/geo.ts) because it's a UI concern. If TRIP-7 needs the same formatting on the map summary line, lift it to `lib/format.ts` rather than re-importing from a component file.
---

